#include <stdio.h>
/**
 * main - Entry point
 * Return: Always zero (success)
 */
int main(void)
{
	char C;

	int d;

	C = 'a';
	while
		(d < 10) {
			putchar(d + '0');
			d++;
		}
	while
		(C <= 'f') {
			putchar(C);
			C++;
		}
	putchar('\n');
	return (0);
}
