#include <stdio.h>
/**
 * main - Entry point
 * Return: Always zero (success)
 */

int main(void)
{

	char C;

	C = 'z';
	while

		(C >= 'a') {
			putchar(C);
			C--;

		}
putchar('\n');
return (0);
}
